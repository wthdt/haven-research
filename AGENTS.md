# 避风港项目工作约定

## 开始工作前

- 先阅读 `SPEC.md`；它是项目迁移与重建的主规格。
- 再阅读 `README.md` 和相关 `config/*.yaml`。
- 发生冲突时，遵循 `SPEC.md` 中的“文档证据优先级”，不得用旧报告或讨论性文字覆盖当前代码与配置事实。

## 安全边界

- 本项目仅限研究、回测和 Shadow。
- 不连接券商，不创建、修改或撤销 Paper/Live/真实订单。
- 不得把 TQQQ 启用于 Paper 或 Live。
- 不得创建或启用 Haven Cron，除非用户明确要求，并且相关测试与验收已经通过。
- `N` 新闻模型只能作为 Shadow 观察层，不得影响仓位。
- 外部数据缺失、过期或覆盖不足时必须保留 `DATA_GUARD`，不得用猜测值绕过。

## 模型约束

- 所有计算必须保持无前视。
- 交易日收盘后计算信号，目标仓位最早在下一交易日生效。
- 不得用当前成分股或当前期权链回填历史。
- 修改公式、参数、执行假设或数据口径时，同时更新配置、测试和 `SPEC.md`。
- 保留 `R_put / R_call / R` 与保险评分的 Audit 可重建性。

## 验证

- 推荐使用 Python 3.11 或更高版本。
- 基础测试：

  ```bash
  python3 -m unittest discover -s tests -v
  ```

- 回测或输出发生变化时，说明数据版本、配置、评估区间、费用和滑点假设，并与现有基线对比。
- 不覆盖 `data/raw/` 或权威验收结果，除非任务明确要求刷新，并保留来源与日期。
